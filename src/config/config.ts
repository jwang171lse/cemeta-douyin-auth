const G = {
	ENV: import.meta.env.MODE,
	defaultTitle: ' ',
}

export default G

export const DOMAIN_MAP = {
	default: import.meta.env.VITE_BASE_URL,
}
export const DOMAIN_DOUYIN = {
	default: import.meta.env.VITE_BASE_DOUYIN_URL,
}

